# Workspace Rules

- Treat this directory as the template root for user home workspaces.
- Only `AGENTS.md` and `skills/` are copied into each home workspace seed.
- Keep symlinks out of template content.
